import { Box } from '@mui/material'
import { Props } from './Payment.interface'

export const Stripe = ({ offer, closeWithResult }: Props) => {
  console.log(offer, closeWithResult)
  return <Box>Stripe</Box>
}
